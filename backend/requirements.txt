fastapi==0.104.1
uvicorn[standard]==0.24.0
numpy>=1.26.4
pandas>=2.1.4
pydantic==2.4.2
python-multipart==0.0.6

# System monitoring — required by main.py for CPU/memory/disk stats
psutil>=5.9.0

# PDF report generation — required by utils/pdf_generator.py
reportlab>=4.0.0

# Filesystem watching for uvicorn --reload
watchfiles>=0.21.0

# ML (optional — uncomment if you have TensorFlow installed)
# tensorflow>=2.16.0
# scikit-learn>=1.4.0

# Windows only — required for Layer 2 (WMI) SMART reading
# Install on Windows: pip install pywin32
# pywin32>=306
